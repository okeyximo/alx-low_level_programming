#include <stdio.h>
#include "main.h"

/**
 * add - adds two integers and returns the result
 * @a: first parameter
 * @b: second parameter
 *
 * Return: the result
 */

int add(int a, int b)
{
	return (a + b);
}
