#include <stdio.h>

/**
 * main - Prints all single base ten numbers
 *
 * Return: 0 (Success)
 */

int main(void)
{
	int i = 0;

	while (i < 10)
	{
		putchar(i);
		i++;
		putchar('\n');
	
	}
	return (0);
}
